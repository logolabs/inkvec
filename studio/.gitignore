node_modules/
dist/
src-tauri/target/
src-tauri/gen/
# Built by scripts/sidecar.mjs at build time from crates/inkvec-cli, so the command the
# app installs is always the engine in this tree. Never committed.
src-tauri/binaries/
