{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "The splash window listens for start-up progress, says when its animation is over, and can be dragged. It reads no file, opens no URL and cannot call any command.",
  "windows": ["splash"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:window:allow-start-dragging"
  ]
}
