name: docs
on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "studio/public/guide/img/**"
      - "web/logo.svg"
      - "web/favicon.svg"
      - "README.md"
      - "PIPELINE_EXPLANATION.md"
      - "CHANGELOG.md"
      - "CONTRIBUTING.md"
      - "SECURITY.md"
      - "CODE_OF_CONDUCT.md"
      - "tools/build_site.py"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install markdown==3.5.1 pygments==2.19.1  # as studio.yml, so the site and the app's guide render alike
      - run: python tools/build_site.py --base /inkvec/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
