{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "npm run build && node scripts/stage-ort.mjs && npm run sidecar"
  },
  "bundle": {
    "resources": {
      "ort-runtime/onnxruntime.dll": "onnxruntime.dll",
      "ort-runtime/onnxruntime_providers_shared.dll": "onnxruntime_providers_shared.dll"
    }
  }
}
