// swift-tools-version:5.9
//
// Inkvec @VERSION@ for Swift. GENERATED by packages/swift/scripts/render_mirror.py in
// https://github.com/logolabs/inkvec from packages/swift/mirror/Package.swift.in; this
// repository is a mirror of packages/swift there. Do not edit it here.
//
// On macOS hosts (every build for macOS and iOS), the C library is a prebuilt static
// XCFramework. On other hosts, Linux in particular, it is the system library `inkvec_ffi`:
// see "Linux" in README.md.

import PackageDescription

#if os(macOS)
let ffi: Target = @BINARY_TARGET@
#else
let ffi: Target = .systemLibrary(name: "InkvecFFI", path: "Sources/InkvecFFI")
#endif

let package = Package(
    name: "Inkvec",
    platforms: [.macOS(.v10_15), .iOS(.v13)],
    products: [
        .library(name: "Inkvec", targets: ["Inkvec"]),
    ],
    targets: [
        ffi,
        .target(name: "Inkvec", dependencies: ["InkvecFFI"], path: "Sources/Inkvec"),@TEST_TARGET@
    ]
)
